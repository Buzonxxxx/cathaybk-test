const { config } = require('./shared.conf')

config.specs = [
    '.././test/specs/ios/test.js'
];

config.capabilities = [
    {
        // To implement
    }]

config.port = 4723

exports.config = config;